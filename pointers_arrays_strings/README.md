# Pointers, arrays, strings

This project introduces pointers in C — variables that store memory addresses — along with arrays and strings, and how these three concepts relate to and build on one another.
