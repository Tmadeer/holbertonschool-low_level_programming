# More functions, nested loops

This project builds on functions and nested loops in C, covering character-checking functions, string-printing helpers, and more complex nested-loop patterns — all built without the standard library, using a custom `_putchar` function.
