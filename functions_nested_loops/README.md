# Functions, nested loops

This project covers writing custom functions in C, understanding declarations vs definitions, prototypes, variable scope, and nested loops — building everything without the standard library, using a custom `_putchar` function.
